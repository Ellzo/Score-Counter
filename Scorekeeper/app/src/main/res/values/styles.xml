<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="buttons_style" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/button_back</item>
        <item name="android:layout_width">@dimen/button_dimn</item>
        <item name="android:layout_height">@dimen/button_dimn</item>
    </style>

    <style name="plus_buttons_style" parent="buttons_style">
        <item name="android:src">@drawable/plus_button_selector</item>
        <item name="android:contentDescription">@string/plus_button</item>
    </style>

    <style name="minus_buttons_style" parent="buttons_style">
        <item name="android:src">@drawable/minus_button_selector</item>
        <item name="android:contentDescription">@string/minus_button</item>
    </style>

    <style name="txt_style">
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Headline</item>
    </style>

</resources>
